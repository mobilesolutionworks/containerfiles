name: work

on:
  push:
    branches: [ main, develop ]

jobs:
  build:
    name: build
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build
        run: |
          gradle build-all
        shell: sh